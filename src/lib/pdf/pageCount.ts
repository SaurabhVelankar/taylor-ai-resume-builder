/**
 * STUB: Read real PDF page count (pdfinfo / pdf-lib).
 * This is the hard gate — never replace with character estimates.
 */
export async function getPdfPageCount(
  _pdfPath: string | null,
): Promise<number | null> {
  // null = unknown because compile is stubbed
  return null;
}
